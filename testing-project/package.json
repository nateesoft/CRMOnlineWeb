{
  "scripts": {
    "cypress:local": "cypress open --env configFile=local",
    "cypress:run": "cypress run --env configFile=local"
  },
  "dependencies": {
    "cypress": "^10.0.3",
    "mysql2": "^2.3.3",
    "mochawesome": "^7.1.3"
  },
  "devDependencies": {
  }
}
