/*
 * ClientRegister Messages
 *
 * This contains all the text for the ClientRegister container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ClientRegister';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ClientRegister container!',
  },
});
